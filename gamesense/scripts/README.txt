这是唯一「真下载」的分类。目录结构：gamesense/scripts/<脚本名>/<版本>/
例：gamesense/scripts/pack/4.0/pack.zip

- 每个版本是一个文件夹，里面放该版本的下载文件（+ 可选预览图）。
- 客户端只显示 Download 按钮，用户必须先选游戏根目录才能下载。
- 生成清单时脚本会给每个版本的文件算 sha256，客户端下载后校验，不匹配会删除文件。
- 多版本就放多个版本文件夹（4.0/、4.1/），聚成版本下拉。

这个 README.txt 会被生成脚本忽略。放好真实文件后重新双击「生成清单.cmd」。
